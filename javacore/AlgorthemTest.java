package javacore;

public class AlgorthemTest {
    /*
    Collections实现了快速排序 洗牌 二分查找算法
    批操作 removeAll addAll
    集合与数组转换
     */
    /*
    遗留的集合
    HashTable  并发访问，已经升级为 ConcurrentHashMap
    枚举 Enumeration接口

    属性映射 property map
    键值都是字符串
    可以保存到文件里，或从文件里加载
    使用一个默认的辅助表，称为Properties

     栈
     栈为空时，不能pop和peek
     */
    /*
    位集 BitSet
    用于高效低存储位序列
    可以进行位运算 或 or 与and 非Not 异或xor
     */
}
